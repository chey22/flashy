<%# "create comment" form partial to use in the render of the show page %>
<%# form_with call here uses an array, which will build a nested route, such as /articles/1/comments %>
<%= form_with model: [ @article, @article.comments.build ] do |form| %>
    <p>
        <%= form.label :commenter %>
        <br>
        <%= form.text_field :commenter %>
    </p>
    <p>
        <%= form.label :body %>
        <br>
        <%= form.text_area :body %>
    </p>
    <%# add a select box to the forms, and let the user select the status when they create a new article or post a new comment.
    We can also specify the default status as public %>
    <p>
        <%= form.label :status %>
        <br>
        <%= form.select :status, ['public', 'private', 'archived'], selected: 'public' %>
    </p>
    <p>
        <%= form.submit %>
    </p>
<% end %>