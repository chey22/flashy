<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting!

<ul>
    <% @articles.each do |article| %>
        <%# use the `archived?`` method to avoid displaying any article that is archived %>
        <% unless article.archived? %>
            <li>
            <%# let's add a convenient way to get to an article's page, so %>
            <%# link each article's title to its respective page %>
            <%#  if we pass an article, link_to will call article_path %>
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <% end %>
</ul>

<%# takes you to http://localhost:3000/articles/new %>
<%= link_to "New Article", new_article_path %>